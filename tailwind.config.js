/** @type {import('tailwindcss').Config} */
module.exports = {
  content: ["./src/**/*.{html,js}"],
  // this config means that this content attributes takes an array, list takes an all the files where icon used for tailwind css
  theme: {
    extend: {},
  },
  plugins: [],
}

